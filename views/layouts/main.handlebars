<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>App</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: sans-serif;
            color: #333;
        }

        input,
        p,
        textarea,
        button {
            font-size: 16px;
        }

        .alert {
            display: block;
            padding: 20px;
            border-radius: 10px;
            background: #fff;
            color: #333;
        }

        .alert.success {
            background: rgba(0, 200, 0, 0.5);
        }

        .alert.error {
            background: rgba(200, 00, 0, 0.5);
        }

        .flex-center {
            display: flex;
            justify-content: center;
            align-items: center;
            background: #eee;
            height: 100vh;
        }

        input {
            padding: 10px;
            border: 1px solid #eee;
            border-radius: 5px;
            color: #555;
            margin-bottom: 10px;
            width: auto;
        }

        .btn {
            box-shadow: none;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background: rgb(30, 33, 77);
            color: #fff;
        }

        .form {
            width: 400px;
            background: #fff;
            padding: 20px;
            padding-bottom: 50px;
            display: flex;
            flex-direction: column;
            border-radius: 10px;
            box-shadow: 20px 20px 100px 0 rgba(0, 0, 0, 0.3);
        }

        .text-center {
            text-align: center;
        }

        .column {
            flex-direction: column;
        }
    </style>
</head>

<body>
    {{{ body }}}
</body>

</html>